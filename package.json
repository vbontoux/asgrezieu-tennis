{
  "name": "asgrezieu-tennis",
  "type": "module",
  "version": "0.1.0",
  "description": "Site web du club AS Grézieu Tennis",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "tinacms dev -c \"astro dev\"",
    "build": "astro build",
    "build:with-admin": "tinacms build --local --skip-cloud-checks && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@tinacms/auth": "^1.1.3",
    "@tinacms/cli": "^2.3.1",
    "astro": "^6.3.3",
    "tinacms": "^3.8.1"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
